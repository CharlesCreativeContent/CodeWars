/*
Create a function named combineNames(combine_names in python, ruby, CombineNames in C#) that accepts two parameters (first and last name). The function should return the full name.

Example:

combineNames('James', 'Stevens')
returns:

'James Stevens'
*/

//Answer//

let combineNames = (a,b)=>a+' '+b
